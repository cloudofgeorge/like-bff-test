const { getArgv } = require('./get-argv');
const { fastFilterArray } = require('./fast-filter-array');
const { deepCopy } = require('./deep-copy');

module.exports = { getArgv, fastFilterArray, deepCopy };
